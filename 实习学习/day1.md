1. 在 Go 语言中，使用切片字面量的方式创建切片时，切片的大小（长度）和容量初始时是相等的
2. 在Go语言中，切片作为参数传递时，是传值，虽然传递的是切片的副本，但由于副本和原切片都指向同一个底层数组，所以在函数内部对切片原始的修改会反映到原切片上。但是，函数内部对切片长度（通过append等操作增加长度）或容量的修改不会影响原切片的长度和容量（因为使用append增加切片长度或容量时，实际上是创建了一个新的切片，导致原切片不会受影响）
3. go中变量定义了要使用，不然会报错